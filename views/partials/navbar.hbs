<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <div class="container">
    <a class="navbar-brand" href="/">
      <img src="/img/logo.png" alt="Logo" style="width:130px;">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#main-navbar"
      aria-controls="main-navbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link {{active path=path match='/restaurants'}}" aria-current="page" href="/restaurants">Restaurants</a></li>
        {{#if currentUser }}
          <li class="nav-item"><a class="nav-link {{active path=path match='/restaurants/new'}}" href="/restaurants/new">Add New Restaurant</a></li>
        {{/if}}
      </ul>
      <ul class="navbar-nav">
        {{#if currentUser }}
          <li class="nav-item"><a class="nav-link {{active path=path match='/profile'}}" aria-current="page" href="/profile">{{ currentUser.email }}</a></li>
          <li class="nav-item"><a class="nav-link" aria-current="page" href="/logout">Logout</a></li>
          <img src="{{currentUser.avatar}}"/>
        {{else}}
          <li class="nav-item"><a class="nav-link {{active path=path match='/register'}}" aria-current="page" href="/register">Register</a></li>
          <li class="nav-item"><a class="nav-link {{active path=path match='/login'}}" href="/login">Login</a></li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>